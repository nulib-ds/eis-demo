<Interstitials.Hero
  headline="Loring Park neighborhood today"
  description="Today, Loring Park neighborhood is a thriving area of Minneapolis."
  background="theme"
  random={true}
  links={[
    {
      href: "/about/get-started",
      title: "sample 1",
      type: "primary",
    },
    {
      href: "https://canopy-iiif.github.io/app/docs/",
      title: "sample 2",
      type: "secondary",
    },
  ]}
/>

<Container>

## Demographics 

population in 1970 (incl. areas not subject to redevelopment) was 1387 (from EIS document)

in re-developable portion of the district was about 1000

population in 1960 was 1968 (from EIS document)

2020 population: 9,718 (source: Wikipedia)

<Image
  src="https://iiif.dc.library.northwestern.edu/iiif/3/ac3a5c6d-695a-4806-b7e0-9f22b5dab7ab/full/3000,/0/default.jpg"
/>

<div id="scroll-chart" className="space-y-4 max-w-2xl my-8">
  <div className="relative h-8 bg-gray-200 rounded overflow-hidden">
    <div className="bar-fill h-full bg-blue-600 w-0 transition-all duration-1000" data-target="85%"></div>
    <span className="absolute inset-y-0 left-2 flex items-center text-xs font-bold text-black">Category A</span>
  </div>
  <div className="relative h-8 bg-gray-200 rounded overflow-hidden">
    <div className="bar-fill h-full bg-blue-600 w-0 transition-all duration-1000" data-target="60%"></div>
    <span className="absolute inset-y-0 left-2 flex items-center text-xs font-bold text-black">Category B</span>
  </div>
</div>

{/* MDX-Safe Trigger */}
{typeof window !== 'undefined' && (
  <script dangerouslySetInnerHTML={{ __html: `
    (function() {
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.querySelectorAll('.bar-fill').forEach(bar => {
              bar.style.width = bar.getAttribute('data-target');
            });
          }
        });
      }, { threshold: 0.1 });

      const target = document.querySelector('#scroll-chart');
      if (target) observer.observe(target);
    })();
  `}} />
)}

</Container>
